services:

    php:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: ip_php
        volumes:
            - .:/var/www:cached
        working_dir: /var/www
        depends_on:
            - db

    nginx:
        image: nginx:latest
        container_name: ip_nginx
        ports:
            - "8082:80"
        volumes:
            - .:/var/www:cached
            - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
        depends_on:
            - php

    db:
        image: mysql:8.0
        container_name: ip_mysql
        restart: always
        environment:
            MYSQL_ROOT_PASSWORD: root
            MYSQL_DATABASE: laravel
            MYSQL_USER: test
            MYSQL_PASSWORD: test
        ports:
            - "33063:3306"
        volumes:
            - database_data:/var/lib/mysql

volumes:
    database_data:
